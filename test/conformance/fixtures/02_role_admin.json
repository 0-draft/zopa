{
  "name":  "role_admin",
  "rego":  "package authz\n\ndefault allow = false\n\nallow if input.user.role == \"admin\"\n",
  "cases": [
    { "input": { "user": { "role": "admin" } }, "expected": 1 },
    { "input": { "user": { "role": "guest" } }, "expected": 0 },
    { "input": {},                                "expected": 0 }
  ]
}
